{
  "name": "@understoria/desktop",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "description": "Understoria desktop shell — the PWA in a carryable Linux AppImage, no installed browser required.",
  "author": "Understoria Contributors",
  "main": "dist/main.js",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "prepare:web": "node scripts/prepare-web.mjs",
    "start": "electron .",
    "dist": "npm run prepare:web && npm run build && electron-builder --linux AppImage"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "electron": "43.1.1",
    "electron-builder": "^26.15.3",
    "typescript": "^5.6.3",
    "vitest": "^4.1.9"
  }
}
