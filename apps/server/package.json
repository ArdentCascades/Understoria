{
  "name": "@understoria/server",
  "version": "0.3.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Understoria community node — Fastify + SQLite. Receives signed exchanges, verifies them with @understoria/shared, stores them for federation.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/helmet": "^12.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@understoria/shared": "*",
    "better-sqlite3-multiple-ciphers": "^12.11.1",
    "fastify": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^4.1.9"
  }
}
